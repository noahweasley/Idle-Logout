name: idle_logout
description: Automatic inactivity-based logout trigger for Flutter applications with configurable timeout detection.
version: 2.0.0
homepage: https://pub.dev/packages/idle_logout
repository: https://github.com/noahweasley/idle_logout

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.35.0"

platforms:
  android:
  ios:
  web:
  linux:
  macos:
  windows:

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^10.0.0
